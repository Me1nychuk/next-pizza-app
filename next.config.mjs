/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["media.dodostatic.net", "cdn.dodostatic.net", "localhost"],
  },
  reactStrictMode: false,
};

export default nextConfig;
